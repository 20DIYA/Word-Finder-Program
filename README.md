# Word-Finder-Program
Word Finder Program is a simple command-line tool written in Go that allows you to search for words within a predefined string (corpus). It will return the index of the word found in the string, making it easy to locate specific words in a block of text.
